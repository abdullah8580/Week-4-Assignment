"use strict";
// Exercise 2: Write a program that takes input and calculates the volume of a cube.
function cubeVolumeFinder(num1) {
    return num1 * num1 * num1;
}
let cubeSide = 10;
let volumeResult = cubeVolumeFinder(cubeSide);
console.log(volumeResult);
