// Exercise 1:  Write a program that calculates the area of a rectangle.

function multiply(num1: number, num2: number): number {
  return num1 * num2;
}

let width: number = 10;
let height: number = 30;

let result: number = multiply(width, height);
console.log(result);
